{
  "name": "kpremium-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"npx serve .\"",
    "build": "tinacms build",
    "start": "npx serve ."
  },
  "dependencies": {
    "tinacms": "latest",
    "@tinacms/cli": "latest"
  }
}
